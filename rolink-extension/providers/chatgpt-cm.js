// world:MAIN — tap CodeMirror editor before render truncates long lines
(function(){
  try{
    const origFetch=window.fetch;
    // no-op, just ensure MAIN world loads; real parser reads editor.value
  }catch{}
})();
